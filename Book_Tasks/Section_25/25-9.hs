-- Да се дефинира функция flattenl :: [[a]] → [a], която получава списък от
-- списъци и връща списък, който съдържа всички елементи на входните
-- списъци.

flatten :: [[a]] -> [a]
flatten [] = []  
flatten (x:xs) = x ++ flatten xs  


